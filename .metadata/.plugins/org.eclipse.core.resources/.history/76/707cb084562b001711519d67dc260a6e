import java.util.*;

public class WeightCompare implements Comparator<Integer>{
	
	public int compare(Integer weightV, Integer weightW) {
		if (weightV.intValue() >= weightW.intValue()) {
			return 1;
		} else {
			return -1;
		}
	}
}
